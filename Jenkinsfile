pipeline {
    agent any

    stages {
           stage('Clone sources') {
               steps {
                git url: 'https://github.com/jfrogdev/project-examples.git'
               }
           }
    }
}
