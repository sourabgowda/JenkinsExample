pipeline {
    agent any

    stages {
        stage('Junit') {
            steps {
                echo 'run junits'
            }
        }
        stage('Build') {
            steps {
                echo 'building the project'
            }
        }
    }
}
